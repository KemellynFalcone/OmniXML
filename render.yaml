services:
  - type: web
    name: omnixml-web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-prod.txt
    startCommand: gunicorn web_app_v3:app
    healthCheckPath: /health
    autoDeployTrigger: commit
