name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}
      - name: Materializar Dashboard UX v24
        if: github.event_name == 'pull_request' && github.head_ref == 'ux-dashboard-v24-welcome-preview'
        run: |
          python tools/apply_dashboard_v24.py
          if ! git diff --quiet -- templates/dashboard.html; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add templates/dashboard.html
            git commit -m "feat: materializa Dashboard UX v24"
            git push origin HEAD:${{ github.head_ref }}
          fi
      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements-dev.txt
      - name: Audit dependencies
        run: pip-audit -r requirements-prod.txt
      - name: Run tests
        run: pytest -q
